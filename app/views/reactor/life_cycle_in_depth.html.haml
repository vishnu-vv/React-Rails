%span.home-message
%br/

= react_component('ComponentLifeCycle', name: 'GorbyPuff')

%pre
  %code
    :preserve
    :escaped

      // reactor/life_cycle_in_depth.html.haml
      = react_component('ComponentLifeCycle', name: 'GorbyPuff')

      // app/assets/components/life_cycle_in_depth.es6.jsx
      class ComponentLifeCycle extends React.Component {<br/><br/>  constructor(props) {<br/>    super(props);<br/>    this.state = { name: 'Vipul' };<br/>  }<br/><br/>  render() {<br/>    return (&lt;div&gt;<br/>      &lt;h1&gt;Hello, {this.props.name}&lt;/h1&gt;<br/>      &lt;h1&gt;My name is, {this.state.name}&lt;/h1&gt;<br/>    &lt;/div&gt;);<br/>  }<br/>}<br/><br/>ComponentLifeCycle.propTypes = {<br/>  name: React.PropTypes.string.isRequired,<br/>}
